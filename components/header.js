import { Authenticator } from "@aws-amplify/ui-react"

import Nav from "./Nav"

const Header = () => {
  return (
    <header>

      
        <Nav/>
      
    </header>
  )
}

export default Header